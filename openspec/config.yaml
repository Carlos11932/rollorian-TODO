schema: spec-driven

context: |
  Tech stack: None detected yet; repository is an empty Git workspace.
  Architecture: Not established.
  Testing: No test runner, coverage tool, or test layers detected.
  Style: No linter, formatter, or type checker detected.
  Conventions: No project-level agent or instruction files detected.

strict_tdd: false

testing:
  detected: 2026-04-14
  strict_tdd: false
  note: Strict TDD Mode unavailable — no test runner detected.
  test_runner:
    available: false
    framework: "—"
    command: "—"
  layers:
    unit:
      available: false
      tool: "—"
    integration:
      available: false
      tool: "—"
    e2e:
      available: false
      tool: "—"
  coverage:
    available: false
    command: "—"
  quality_tools:
    linter:
      available: false
      command: "—"
    type_checker:
      available: false
      command: "—"
    formatter:
      available: false
      command: "—"

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    - Load relevant coding skills for the project stack
  verify:
    - Run tests if test infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas (large removals)
